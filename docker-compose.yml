

services:
  # Frontend Vue
  frontend:
    build:
      context: .
      args:
        BUILD_MODE: development
    container_name: vue-frontend
    ports:
      - "8080:3000"  # Accedes por http://localhost:8080
    networks:
      - papermill-procurement_papermill-network
    restart: unless-stopped

networks:
  papermill-procurement_papermill-network:
    driver: bridge